import type { Context } from '@deepseek-ai/cordis'

export const name = 'dsh-icon'
export const inject = []

export function apply(_ctx: Context): void {
  // Host-side plugin: purely runs in client Web UI
}
